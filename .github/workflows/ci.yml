name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      # Las pruebas levantan el servidor en un puerto efímero de 127.0.0.1;
      # no necesitan servicios externos ni variables de entorno.
      - name: Tests
        run: bun test

      - name: Build
        run: bun run build
